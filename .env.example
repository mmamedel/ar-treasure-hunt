# Database Configuration
# For local development (using Docker Compose):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ar_treasure_hunt"

# Note: For production, set DATABASE_URL in Vercel's environment variables
# with your Prisma Accelerate connection string:
# DATABASE_URL="prisma://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"
